<div class="container" id="game_card">
  <div id="next_card_btn">Next Card</div>
  <% @deck.cards.each_with_index do |card, i| %>
  <div class="hidden">
    <div id="definition_<%= i %>">Definition: <%= card.definition %></div>
    <div id="answer_<%= i %>">Answer: <%= card.answer %></div>
  </div>
    <br>
  <% end %>
</div>
